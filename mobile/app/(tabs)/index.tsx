import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function FeedScreen() {
  return <ScreenPlaceholder eyebrow="REDE ESPORTIVA" title="Feed" description="A fundação da timeline mobile está pronta para receber publicações, Stories e Reels do mesmo backend usado pelo site." />;
}
