import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function ProfileScreen() {
  return <ScreenPlaceholder eyebrow="IDENTIDADE ÚNICA" title="Meu Perfil" description="Esta tela será a primeira integração Web ↔ App: o mesmo Firebase UID e o mesmo documento perfis/{uid} em ambas as plataformas." />;
}
