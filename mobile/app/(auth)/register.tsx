import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function RegisterScreen() {
  return <ScreenPlaceholder eyebrow="NOVA CONTA" title="Criar conta" description="Cadastro mobile usará o mesmo Firebase Auth UID do site e criará a conta/perfil sem duplicar o cadastro legado de atletas." />;
}
