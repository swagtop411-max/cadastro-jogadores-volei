import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function ForgotPasswordScreen() {
  return <ScreenPlaceholder eyebrow="RECUPERAÇÃO" title="Recuperar senha" description="A recuperação reutilizará o Firebase Authentication para manter o mesmo fluxo de credenciais entre site e aplicativo." />;
}
