import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function LoginScreen() {
  return <ScreenPlaceholder eyebrow="ACESSO" title="Entrar" description="O formulário será conectado ao Firebase Auth nativo e ao App Check antes do primeiro teste de identidade cruzada com o site." />;
}
