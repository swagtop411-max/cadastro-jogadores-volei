name: Apply Mobile V30 Firestore Rules

on:
  push:
    branches: ["feature/mobile-store-ready-v30"]
    paths:
      - "scripts/append-mobile-v30-rules.mjs"
      - ".github/workflows/mobile-v30-apply-rules.yml"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v7
        with:
          ref: feature/mobile-store-ready-v30
          fetch-depth: 0

      - name: Apply idempotent rules patch
        run: node scripts/append-mobile-v30-rules.mjs

      - name: Commit patched firestore.rules
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- firestore.rules; then
            echo "No rules changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add firestore.rules
          git commit -m "feat(rules): proteger consentimento legal do app V30"
          git push origin HEAD:feature/mobile-store-ready-v30
