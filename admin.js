import{initializeApp}from"https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js";import{getAuth,onAuthStateChanged,signInWithEmailAndPassword,sendPasswordResetEmail,signOut}from"https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js";import{getFirestore,collection,addDoc,getDocs,updateDoc,deleteDoc,doc,setDoc,getDoc,serverTimestamp,query,orderBy,where}from"https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js";
const firebaseConfig={apiKey:"AIzaSyBMsuR0320Nz3asVRj5axXFvKJ5Ftz9COQ",authDomain:"jogadores-de-volei.firebaseapp.com",projectId:"jogadores-de-volei",storageBucket:"jogadores-de-volei.firebasestorage.app",messagingSenderId:"48728914064",appId:"1:48728914064:web:1dd7aeb705319886f74015",measurementId:"G-K033D1K41Y"};
const app=initializeApp(firebaseConfig),auth=getAuth(app),db=getFirestore(app);const $=id=>document.getElementById(id);const campeonatosContainer=$("campeonatosContainer"),loginSection=$("loginSection"),adminSection=$("adminSection"),loginForm=$("loginForm"),loginStatus=$("loginStatus"),adminStatus=$("adminStatus"),atletaForm=$("atletaForm"),atletaId=$("atletaId"),foto=$("foto"),fotoPreview=$("fotoPreview"),historicoContainer=$("historicoContainer"),atletasAdmin=$("atletasAdmin"),apoiadorForm=$("apoiadorForm"),apoiadorStatus=$("apoiadorStatus"),apoiadoresAdmin=$("apoiadoresAdmin"),apoiadoresPendentes=$("apoiadoresPendentes"),apoiadoresPendentesStatus=$("apoiadoresPendentesStatus"),apoiadoresPendentesBadge=$("apoiadoresPendentesBadge");let atletas=[],fotoAtual="",apoiadores=[],apoiadorImagemAtual="";
const normalizarCategoria=v=>{const s=String(v||"").trim().toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"");if(s.includes("inic"))return"Iniciante";if(s.includes("avan"))return"Avançado";if(s.includes("inter"))return"Intermediário";return String(v||"").trim()};const UFS=new Set(["AC","AL","AP","AM","BA","CE","DF","ES","GO","MA","MT","MS","MG","PA","PB","PR","PE","PI","RJ","RN","RS","RO","RR","SC","SP","SE","TO"]);const normalizarCidade=v=>{let s=String(v||"").trim().replace(/\s+/g," ");let m=s.match(/^([A-Z]{2})\s*[-,]\s*(.+)$/i);if(m&&UFS.has(m[1].toUpperCase())){let cidade=m[2].trim().replace(/\s*[-,]\s*[A-Z]{2}$/i,"");return m[1].toUpperCase()+" - "+cidade}m=s.match(/^(.+?)\s*[-,]\s*([A-Z]{2})$/i);if(m&&UFS.has(m[2].toUpperCase()))return m[2].toUpperCase()+" - "+m[1].trim();return s};const notasIds=["Saque","Recepcao","Levantamento","Ataque","Bloqueio","Defesa"];function status(el,msg,t="ok"){el.textContent=msg;el.className=`status ${t}`}function clear(el){el.textContent="";el.className="status"}function esc(v){const d=document.createElement("div");d.textContent=v??"";return d.innerHTML}function normalizeUrl(v){let x=(v||"").trim();if(x&&!/^https?:\/\//i.test(x))x="https://"+x;return x}
function nota(id){return Number($("nota"+id)?.value||0)}function getAvaliacao(){const out={};notasIds.forEach(n=>out[n.toLowerCase()]=nota(n));const vals=Object.values(out).filter(v=>v>0);out.media=vals.length?Number((vals.reduce((a,b)=>a+b,0)/vals.length).toFixed(2)):0;return out}function setAvaliacao(v={}){notasIds.forEach(n=>{$("nota"+n).value=String(Number(v[n.toLowerCase()]||0))});atualizarMedia()}function atualizarMedia(){const vals=notasIds.map(n=>nota(n)).filter(v=>v>0),media=vals.length?vals.reduce((a,b)=>a+b,0)/vals.length:0;$("mediaTecnica").textContent=media?media.toFixed(1):"Não avaliado";$("estrelasTecnicas").textContent=media?"★".repeat(Math.round(media))+"☆".repeat(5-Math.round(media)):""}function addCampeonato(nome="",colocacao="",ano=""){const d=document.createElement("div");d.className="historico-item campeonato-item";d.innerHTML=`<input class="campNome" placeholder="Campeonato" value="${esc(nome)}"><input class="campColocacao" placeholder="Colocação (ex.: 1º lugar)" value="${esc(colocacao)}"><input class="campAno" type="number" min="1900" max="2100" placeholder="Ano" value="${esc(ano)}"><button type="button" class="btn-danger">❌</button>`;d.querySelector("button").onclick=()=>d.remove();campeonatosContainer.appendChild(d)}function getCampeonatos(){return[...campeonatosContainer.querySelectorAll(".campeonato-item")].map(d=>({campeonato:d.querySelector(".campNome").value.trim(),colocacao:d.querySelector(".campColocacao").value.trim(),ano:d.querySelector(".campAno").value.trim()})).filter(x=>x.campeonato)}function addHistorico(equipe="",categoria="",ano=""){const d=document.createElement("div");d.className="historico-item";d.innerHTML=`<input class="histEquipe" placeholder="Equipe" value="${esc(equipe)}"><input class="histCategoria" placeholder="Categoria" value="${esc(categoria)}"><input class="histAno" type="number" min="1900" max="2100" placeholder="Ano" value="${esc(ano)}"><button type="button" class="btn-danger">❌</button>`;d.querySelector("button").onclick=()=>d.remove();historicoContainer.appendChild(d)}function getHistorico(){return[...historicoContainer.querySelectorAll(".historico-item")].map(d=>({equipe:d.querySelector(".histEquipe").value.trim(),categoria:d.querySelector(".histCategoria").value.trim(),ano:d.querySelector(".histAno").value.trim()})).filter(x=>x.equipe)}
function resetForm(){atletaForm.reset();atletaId.value="";fotoAtual="";foto.value="";fotoPreview.src="";fotoPreview.style.display="none";$("uf").value="";historicoContainer.innerHTML="";addHistorico();campeonatosContainer.innerHTML="";setAvaliacao({});$("btnSalvar").textContent="Cadastrar atleta";$("btnCancelar").classList.add("hidden")}
function resetApoiadorForm(){apoiadorForm.reset();$("apoiadorId").value="";apoiadorImagemAtual="";$("apoiadorImagem").value="";$("apoiadorPreview").src="";$("apoiadorPreview").style.display="none";$("apoiadorAtivo").checked=true;$("apoiadorPlano").value="Ouro";$("btnSalvarApoiador").textContent="Adicionar apoiador";$("btnCancelarApoiador").classList.add("hidden")}
async function loadNovosCadastros(){const lista=$("novosCadastrosLista"),badge=$("novosCadastrosBadge");if(!lista)return;try{const s=await getDocs(collection(db,"atletas_pendentes"));const arr=s.docs.map(d=>({id:d.id,...d.data()})).sort((a,b)=>String(b.criadoEm||"").localeCompare(String(a.criadoEm||"")));if(badge)badge.textContent=arr.length;if(!arr.length){lista.innerHTML='<p class="subtitulo">Nenhum novo cadastro aguardando análise.</p>';return}lista.innerHTML="";arr.forEach(a=>{const d=document.createElement("div");d.className="atleta-admin novo-card";const mods=Array.isArray(a.modalidades)?a.modalidades.join(" · "):(a.modalidade||"Modalidade não informada");const pago=a.planoId&&a.planoId!=="gratuito";const pagamentoConfirmado=a.pagamentoConfirmado===true;const statusPagamento=pago?(pagamentoConfirmado?"Pagamento confirmado":"⚠️ Aguardando pagamento"):"Plano gratuito";const fotoHtml=a.foto?`<img class="novo-card-photo" src="${esc(a.foto)}" alt="Foto de ${esc(a.nome||"atleta")}" loading="lazy">`:`<div class="novo-card-photo" aria-label="Sem foto">🏐</div>`;d.innerHTML=`${fotoHtml}<div class="atleta-info"><strong>${esc(a.nome||"Sem nome")}</strong><span>${esc(a.cidade||"Sem cidade")} · ${esc(a.categoria||"Sem categoria")}</span><small>${esc(mods)}</small><small style="color:${pago&&!pagamentoConfirmado?"#f2cc72":"#9da39c"};font-weight:800">Plano: ${esc(a.plano||"Gratuito")} · ${esc(statusPagamento)}</small></div><div class="atleta-actions"><button class="btn-secondary ver-novo" type="button">👁️ Ver perfil</button><button class="btn-primary aprovar" type="button">${pago&&!pagamentoConfirmado?"💳 Confirmar pagamento e aprovar":"Aprovar"}</button><button class="btn-danger recusar" type="button">Recusar</button></div>`;d.querySelector(".ver-novo").onclick=()=>abrirPerfilNovo(a);d.querySelector(".aprovar").onclick=()=>aprovarNovoCadastro(a.id);d.querySelector(".recusar").onclick=()=>recusarNovoCadastro(a.id);lista.appendChild(d)})}catch(e){console.error(e);if(badge)badge.textContent="!";lista.innerHTML='<p class="status erro">Não foi possível carregar os novos cadastros.</p>'}}
function abrirPerfilNovo(a){const modal=$("novoCadastroModal"),conteudo=$("novoModalConteudo");if(!modal||!conteudo)return;const mods=Array.isArray(a.modalidades)?a.modalidades.join(" · "):(a.modalidade||"Não informada");const camps=Array.isArray(a.historicoCampeonatos)?a.historicoCampeonatos:[];const campHtml=camps.length?camps.map(x=>'<div class="novo-camp"><strong>'+esc(x.campeonato||"Campeonato")+'</strong> · '+esc(x.colocacao||"Colocação não informada")+(x.ano?' · '+esc(x.ano):"")+'</div>').join(""):"<p>Nenhum campeonato informado.</p>";conteudo.innerHTML='<div class="novo-perfil">'+(a.foto?'<img class="novo-perfil-foto" src="'+esc(a.foto)+'" alt="Foto de '+esc(a.nome||"atleta")+'">':'<div class="novo-perfil-foto" style="display:flex;align-items:center;justify-content:center;font-size:60px">🏐</div>')+'<div><h2 id="novoModalTitulo">'+esc(a.nome||"Sem nome")+'</h2><p><strong>Status:</strong> Cadastro aguardando análise</p><p><strong>Cidade:</strong> '+esc(a.cidade||"Não informada")+'</p><p><strong>Categoria:</strong> '+esc(a.categoria||"Não informada")+'</p><p><strong>Modalidades:</strong> '+esc(mods)+'</p><p><strong>Posição:</strong> '+esc(a.posicao||"Não informada")+'</p><p><strong>Time:</strong> '+esc(a.time||"Não informado")+'</p></div></div><div class="novo-perfil-section"><h3>🏆 Campeonatos</h3>'+campHtml+'</div>'+(a.observacoes?'<div class="novo-perfil-section"><h3>📝 Observações</h3><p>'+esc(a.observacoes)+'</p></div>':"");modal.classList.remove("hidden");document.body.style.overflow="hidden"}function fecharPerfilNovo(){const m=$("novoCadastroModal");if(m){m.classList.add("hidden");document.body.style.overflow=""}}async function aprovarNovoCadastro(id){try{const ref=doc(db,"atletas_pendentes",id),snap=await getDoc(ref);if(!snap.exists())return;const a=snap.data();const pago=a.planoId&&a.planoId!=="gratuito";if(pago&&!a.pagamentoConfirmado){if(!confirm("Confirmar que o pagamento do plano "+(a.plano||"pago")+" foi recebido e publicar este atleta?"))return}else if(!confirm("Aprovar este cadastro e publicar o atleta?"))return;const data={...a,status:"ativo",criadoEm:a.criadoEm||serverTimestamp(),atualizadoEm:serverTimestamp(),pagamentoConfirmado:pago?true:(a.pagamentoConfirmado||false),planoStatus:"ativo"};delete data.contato;await setDoc(doc(db,"atletas",id),data);if(a.contato)await setDoc(doc(db,"atletas",id,"privado","dados"),{contato:a.contato,atualizadoEm:serverTimestamp()},{merge:true});await deleteDoc(ref);await loadNovosCadastros();await loadAtletas();alert(pago?"Pagamento confirmado, plano ativado e atleta publicado com sucesso.":"Cadastro aprovado e publicado com sucesso.")}catch(e){console.error(e);alert("Não foi possível aprovar o cadastro. Verifique as regras do Firestore.")}}
async function recusarNovoCadastro(id){if(!confirm("Recusar e excluir este cadastro pendente?"))return;try{await deleteDoc(doc(db,"atletas_pendentes",id));await loadNovosCadastros()}catch(e){console.error(e);alert("Não foi possível recusar o cadastro.")}}
async function privateData(id){try{const s=await getDoc(doc(db,"atletas",id,"privado","dados"));return s.exists()?s.data():{}}catch{return{}}}
async function loadAtletas(){try{let s;try{s=await getDocs(query(collection(db,"atletas"),orderBy("criadoEm","desc")))}catch{s=await getDocs(collection(db,"atletas"))}const base=s.docs.map(x=>({id:x.id,...x.data()}));atletas=await Promise.all(base.map(async a=>({...a,privado:await privateData(a.id)})));renderAtletas()}catch(e){console.error(e);status(adminStatus,`Erro ao carregar atletas (${e.code||"erro"}).`,"erro")}}
function garantirModalEquipe(){let m=$("equipePendenteModal");if(m)return m;document.body.insertAdjacentHTML("beforeend",'<div id="equipePendenteModal" class="novo-modal hidden" role="dialog" aria-modal="true"><div class="novo-modal-backdrop" data-fechar-equipe-modal></div><div class="novo-modal-box"><button id="fecharEquipeModal" class="novo-modal-close" type="button" aria-label="Fechar">✕</button><div id="equipeModalConteudo"></div></div></div>');m=$("equipePendenteModal");$("fecharEquipeModal").onclick=fecharModalEquipe;m.addEventListener("click",e=>{if(e.target.matches("[data-fechar-equipe-modal]"))fecharModalEquipe()});return m}
function fecharModalEquipe(){const m=$("equipePendenteModal");if(m){m.classList.add("hidden");document.body.style.overflow=""}}
function visualizarEquipePendente(e){const m=garantirModalEquipe(),c=$("equipeModalConteudo");if(!m||!c)return;const logo=e.logo?'<img src="'+esc(e.logo)+'" alt="Logo da equipe" style="width:180px;height:180px;object-fit:contain;border-radius:22px;background:#0b0f0d;border:1px solid rgba(217,169,63,.3);padding:12px">':'<div style="width:180px;height:180px;display:flex;align-items:center;justify-content:center;border-radius:22px;background:#0b0f0d;border:1px solid rgba(217,169,63,.3);font-size:70px">👥</div>';const atletas=Array.isArray(e.atletas)?e.atletas:[];c.innerHTML='<div style="display:grid;grid-template-columns:200px 1fr;gap:24px;align-items:start">'+logo+'<div><h2 style="color:#f2cc72;margin-top:0">'+esc(e.nome||"Sem nome")+'</h2><p><strong>Responsável:</strong> '+esc(e.responsavel||"Não informado")+'</p><p><strong>Local:</strong> '+esc(e.cidade||"Não informado")+'</p><p><strong>Categoria:</strong> '+esc(e.categoria||"Não informado")+'</p><p><strong>Modalidade:</strong> '+esc(e.modalidade||"Não informada")+'</p><p><strong>Plano:</strong> '+esc(e.plano||"Gratuito")+'</p><p><strong>Contato:</strong> '+esc(e.contato||"Não informado")+'</p></div></div><div style="margin-top:22px;padding:16px;border-radius:14px;background:#101613;border:1px solid rgba(217,169,63,.16)"><h3 style="color:#f2cc72;margin-top:0">🏐 Atletas da equipe</h3>'+ (atletas.length?'<ul style="margin:0;padding-left:20px">'+atletas.map(x=>'<li style="margin:6px 0">'+esc(x)+'</li>').join("")+'</ul>':'<p>Nenhum atleta informado.</p>')+'</div>';m.classList.remove("hidden");document.body.style.overflow="hidden"}
async function loadEquipesPendentes(){const lista=$("equipesPendentesLista");if(!lista)return;try{const s=await getDocs(collection(db,"equipes_pendentes"));const arr=s.docs.map(d=>({id:d.id,...d.data()}));if(!arr.length){lista.innerHTML='<p class="subtitulo">Nenhuma equipe aguardando análise.</p>';return}lista.innerHTML=arr.map(e=>{const pago=e.planoId&&e.planoId!=="gratuito",confirmado=e.pagamentoConfirmado===true,statusPg=pago?(confirmado?"Pagamento confirmado":"⚠️ Aguardando pagamento"):"Plano gratuito",logo=e.logo?'<img src="'+esc(e.logo)+'" alt="Logo" style="width:70px;height:70px;object-fit:contain;border-radius:12px;background:#0b0f0d;border:1px solid rgba(217,169,63,.2);padding:5px">':'<div style="width:70px;height:70px;display:flex;align-items:center;justify-content:center;border-radius:12px;background:#0b0f0d;border:1px solid rgba(217,169,63,.2);font-size:28px">👥</div>';return '<div class="atleta-admin"><div style="flex:0 0 70px">'+logo+'</div><div class="atleta-info"><strong>'+esc(e.nome||"Sem nome")+'</strong><span>'+esc(e.cidade||"")+' · '+esc(e.categoria||"")+' · '+esc(e.modalidade||"")+'</span><small>Plano: '+esc(e.plano||"Gratuito")+' · '+Number(e.valorPlano||0).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})+' · '+esc(statusPg)+'</small></div><div class="atleta-actions"><button class="btn-secondary visualizar-equipe" data-id="'+esc(e.id)+'" type="button">👁️ Visualizar</button><button class="btn-primary aprovar-equipe" data-id="'+esc(e.id)+'" type="button">'+(pago&&!confirmado?"💳 Confirmar pagamento e aprovar":"Aprovar")+'</button><button class="btn-danger recusar-equipe" data-id="'+esc(e.id)+'" type="button">Recusar</button></div></div>'}).join("");lista.querySelectorAll(".visualizar-equipe").forEach(b=>b.onclick=()=>{const e=arr.find(x=>x.id===b.dataset.id);if(e)visualizarEquipePendente(e)});lista.querySelectorAll(".aprovar-equipe").forEach(b=>b.onclick=()=>aprovarEquipe(b.dataset.id));lista.querySelectorAll(".recusar-equipe").forEach(b=>b.onclick=()=>recusarEquipe(b.dataset.id))}catch(e){console.error(e);status($("equipesPendentesStatus"),"Não foi possível carregar as equipes.","erro")}}
async function aprovarEquipe(id){try{const s=await getDoc(doc(db,"equipes_pendentes",id));if(!s.exists())return;const a=s.data(),pago=a.planoId&&a.planoId!=="gratuito";if(pago&&!a.pagamentoConfirmado){if(!confirm("Confirmar que o pagamento do plano "+(a.plano||"pago")+" foi recebido e publicar esta equipe?"))return}else if(!confirm("Aprovar e publicar esta equipe?"))return;const data={...a,status:"ativo",aprovacao:"aprovado",planoStatus:"ativo",pagamentoConfirmado:pago?true:(a.pagamentoConfirmado||false),atualizadoEm:serverTimestamp()};await setDoc(doc(db,"equipes",id),data);await deleteDoc(doc(db,"equipes_pendentes",id));await loadEquipesPendentes();await loadMonetizacao();alert(pago?"Pagamento confirmado, plano ativado e equipe publicada com sucesso.":"Equipe aprovada e publicada.")}catch(e){console.error(e);alert("Não foi possível aprovar a equipe. Verifique as regras do Firestore.")}}
async function recusarEquipe(id){if(!confirm("Recusar e excluir esta equipe pendente?"))return;try{await deleteDoc(doc(db,"equipes_pendentes",id));await loadEquipesPendentes()}catch(e){console.error(e);alert("Não foi possível recusar a equipe.")}}
async function loadMonetizacao(){
  const cards=$("monetizacaoCards"),planosBox=$("monetizacaoPlanos"),engBox=$("monetizacaoEngajamento"),st=$("monetizacaoStatus");
  if(!cards)return;
  cards.innerHTML='<div class="monetizacao-loading">🔄 Atualizando dados...</div>';if(st)clear(st);
  const timeout=(p,ms=15000)=>Promise.race([p,new Promise((_,reject)=>setTimeout(()=>reject(Object.assign(new Error("Tempo esgotado"),{code:"deadline-exceeded"})),ms))]);
  try{
    const [spSnap,atSnap,eqSnap,statsSnap,atPendSnap,eqPendSnap]=await Promise.all([
      timeout(getDocs(collection(db,"apoiadores"))),
      timeout(getDocs(collection(db,"atletas"))),
      timeout(getDocs(collection(db,"equipes"))),
      timeout(getDocs(collection(db,"site_stats"))),
      timeout(getDocs(collection(db,"atletas_pendentes"))),
      timeout(getDocs(collection(db,"equipes_pendentes")))
    ]);
    const apoi=spSnap.docs.map(d=>({id:d.id,...d.data()}));
    const atletas=atSnap.docs.map(d=>d.data());
    const equipes=eqSnap.docs.map(d=>d.data());
    const eventos=statsSnap.docs.map(d=>d.data());
    const atPend=atPendSnap.docs.map(d=>d.data());
    const eqPend=eqPendSnap.docs.map(d=>d.data());
    const ativosApoi=apoi.filter(a=>a.ativo!==false);
    const valorPlano=v=>Number(v||0);
    const pagosAtletas=atletas.filter(a=>a.status!=="inativo"&&a.planoId&&a.planoId!=="gratuito"&&a.planoStatus==="ativo");
    const pagosEquipes=equipes.filter(a=>a.status!=="inativo"&&a.planoId&&a.planoId!=="gratuito"&&a.planoStatus==="ativo");
    const valoresApoio={Bronze:50,Prata:100,Ouro:200,Master:350};
    const receitaApoiadores=ativosApoi.reduce((s,a)=>s+Number(a.valor||valoresApoio[a.plano]||0),0);
    const receitaAtletas=pagosAtletas.reduce((s,a)=>s+valorPlano(a.valorPlano),0);
    const receitaEquipes=pagosEquipes.reduce((s,a)=>s+valorPlano(a.valorPlano),0);
    const receita=receitaApoiadores+receitaAtletas+receitaEquipes;
    const planos={Bronze:0,Prata:0,Ouro:0,Master:0};
    ativosApoi.forEach(a=>{const p=String(a.plano||"").trim();if(planos[p]!=null)planos[p]++});
    const visualizacoes=eventos.filter(e=>e.nome==="page_view").length;
    const cliques=eventos.filter(e=>["apoiador_click","apoiador_banner_click"].includes(e.nome)).length;
    const campanhas=ativosApoi.length;
    const anuncios=ativosApoi.filter(a=>String(a.imagem||"").trim()).length;
    const atletasPremium=pagosAtletas.filter(a=>String(a.planoId||"").toLowerCase()==="premium").length;
    const equipesPremium=pagosEquipes.filter(a=>String(a.planoId||"").toLowerCase()==="premium").length;
    const pendentesPagamento=atPend.filter(a=>a.planoId&&a.planoId!=="gratuito"&&a.pagamentoConfirmado!==true).length+
      eqPend.filter(a=>a.planoId&&a.planoId!=="gratuito"&&a.pagamentoConfirmado!==true).length;
    const fmt=n=>Number(n||0).toLocaleString("pt-BR");
    const dinheiro=n=>Number(n||0).toLocaleString("pt-BR",{style:"currency",currency:"BRL"});
    const cardsData=[
      ["💰","Receita mensal",dinheiro(receita),"Apoiadores + atletas + equipes com planos ativos"],
      ["🤝","Patrocinadores ativos",fmt(ativosApoi.length),"Marcas com divulgação ativa"],
      ["📦","Planos",fmt(ativosApoi.length+pagosAtletas.length+pagosEquipes.length),"Contratos pagos ativos"],
      ["🖱️","Cliques",fmt(cliques),"Interações com espaços comerciais"],
      ["👁️","Visualizações",fmt(visualizacoes),"Visualizações registradas"],
      ["📣","Campanhas",fmt(campanhas),"Campanhas comerciais ativas"],
      ["📰","Anúncios",fmt(anuncios),"Apoiadores com material visual"],
      ["🏐","Atletas Premium",fmt(atletasPremium),"Atletas com plano Premium ativo"],
      ["👥","Equipes Premium",fmt(equipesPremium),"Equipes com plano Premium ativo"]
    ];
    cards.innerHTML=cardsData.map(x=>'<div class="monetizacao-card"><span class="ico">'+x[0]+'</span><span class="valor">'+x[2]+'</span><span class="rotulo">'+x[1]+'</span><span class="desc">'+x[3]+'</span></div>').join("");
    planosBox.innerHTML=Object.entries(planos).map(([p,n])=>'<div class="monetizacao-row"><span>'+p+'</span><strong>'+fmt(n)+'</strong></div>').join("");
    engBox.innerHTML='<div class="monetizacao-row"><span>Cliques em apoiadores</span><strong>'+fmt(eventos.filter(e=>e.nome==="apoiador_click").length)+'</strong></div><div class="monetizacao-row"><span>Cliques no banner comercial</span><strong>'+fmt(eventos.filter(e=>e.nome==="apoiador_banner_click").length)+'</strong></div><div class="monetizacao-row"><span>Pagamentos aguardando confirmação</span><strong>'+fmt(pendentesPagamento)+'</strong></div><div class="monetizacao-row"><span>Receita de atletas</span><strong>'+dinheiro(receitaAtletas)+'</strong></div><div class="monetizacao-row"><span>Receita de equipes</span><strong>'+dinheiro(receitaEquipes)+'</strong></div><div class="monetizacao-row"><span>Receita de apoiadores</span><strong>'+dinheiro(receitaApoiadores)+'</strong></div>';
  }catch(e){
    console.error(e);cards.innerHTML='<div class="monetizacao-loading">Não foi possível carregar os dados de monetização.</div>';if(st)status(st,"Erro ao carregar monetização ("+(e.code||"erro")+"). Verifique as permissões do Firestore.","erro");
  }
}
function renderAtletas(){atletasAdmin.innerHTML="";if(!atletas.length){atletasAdmin.innerHTML='<p class="subtitulo">Nenhum atleta cadastrado.</p>';return}atletas.forEach(a=>{const media=Number(a.avaliacaoTecnica?.media||0);const d=document.createElement("div");d.className="atleta-admin";d.innerHTML=`<div class="atleta-info"><strong>${esc(a.nome||"Sem nome")}</strong><span>${esc(a.time||"Sem time")} · ${esc(a.categoria||"Sem categoria")} · ${a.status==="inativo"?"🔴 Inativo":"🟢 Ativo"}</span><small>🔒 ${esc(a.privado?.contato||"Sem contato cadastrado")}</small>${media?`<small>⭐ Técnica: ${media.toFixed(1)}/5</small>`:"<small>⭐ Técnica: não avaliada</small>"}</div><div class="atleta-actions"><button class="btn-secondary editar" data-editar-id="${esc(a.id)}" type="button">✏️ Editar</button><button class="btn-danger excluir" data-excluir-id="${esc(a.id)}" type="button">Excluir</button></div>`;d.querySelector(".editar").addEventListener("click",()=>editAtleta(a.id));d.querySelector(".excluir").addEventListener("click",()=>removeAtleta(a.id));atletasAdmin.appendChild(d)})}
function setUF(cidade){const m=String(cidade||"").match(/^([A-Z]{2})\s*[-,]\s*(.+)$/i);if(m){$("uf").value=m[1].toUpperCase();$("cidade").value=m[2].trim()}else{$("uf").value="";$("cidade").value=cidade||""}}function cidadeComUF(){const uf=$("uf").value.trim().toUpperCase(),cidade=$("cidade").value.trim();return uf&&cidade?`${uf} - ${cidade}`:cidade}function normalizarModalidades(v){if(Array.isArray(v))return v.filter(Boolean);if(typeof v==="string")return v.split(",").map(x=>x.trim()).filter(Boolean);return []}function normalizarPosicoes(v){if(Array.isArray(v))return v.filter(Boolean);if(typeof v==="string")return v.split(/\s*[,/|]\s*/).map(x=>x.trim()).filter(Boolean);return []}function getPosicoes(){return[...document.querySelectorAll('#atletaForm input[name="posicao"]:checked')].map(x=>x.value)}function setPosicoes(v){const pos=normalizarPosicoes(v);document.querySelectorAll('#atletaForm input[name="posicao"]').forEach(x=>x.checked=pos.includes(x.value))}function editAtleta(id){try{const a=atletas.find(x=>x.id===id);if(!a){status(adminStatus,"Atleta não encontrado na lista. Atualize os atletas.","erro");return}atletaId.value=a.id;$("nome").value=a.nome||"";$("nascimento").value=a.nascimento||"";setUF(a.cidade||"");const mods=normalizarModalidades(a.modalidades||a.modalidade);document.querySelectorAll('#atletaForm input[name="modalidade"]').forEach(x=>{x.checked=mods.includes(x.value)});setPosicoes(a.posicoes||a.posicao||"");$("categoria").value=a.categoria||"";$("time").value=a.time||"";$("status").value=a.status||"ativo";$("observacoes").value=a.observacoes||"";$("contato").value=a.privado?.contato||"";fotoAtual=a.foto||"";foto.value="";if(fotoAtual){fotoPreview.src=fotoAtual;fotoPreview.style.display="block"}else{fotoPreview.src="";fotoPreview.style.display="none"}historicoContainer.innerHTML="";(Array.isArray(a.historicoEquipes)?a.historicoEquipes:[]).forEach(h=>addHistorico(h.equipe,h.categoria,h.ano));if(!historicoContainer.children.length)addHistorico();campeonatosContainer.innerHTML="";(Array.isArray(a.historicoCampeonatos)?a.historicoCampeonatos:[]).forEach(h=>addCampeonato(h.campeonato,h.colocacao,h.ano));setAvaliacao(a.avaliacaoTecnica||{});$("btnSalvar").textContent="Salvar alterações";$("btnCancelar").classList.remove("hidden");atletaForm.scrollIntoView({behavior:"smooth",block:"start"});clear(adminStatus)}catch(e){console.error("Erro ao editar atleta:",e);status(adminStatus,"Não foi possível abrir a edição deste atleta. Recarregue a página e tente novamente.","erro")}}
async function removeAtleta(id){const a=atletas.find(x=>x.id===id);if(!a||!confirm(`Excluir "${a.nome}"?`))return;try{await deleteDoc(doc(db,"atletas",id));await deleteDoc(doc(db,"atletas",id,"privado","dados")).catch(()=>{});status(adminStatus,"Atleta excluído com sucesso.");await loadAtletas()}catch(e){status(adminStatus,`Erro ao excluir (${e.code||"erro"}).`,"erro")}}
function compress(file,max=900){return new Promise((resolve,reject)=>{if(!file)return resolve("");const r=new FileReader();r.onload=e=>{const i=new Image();i.onload=()=>{const s=Math.min(1,max/Math.max(i.width,i.height)),c=document.createElement("canvas");c.width=Math.max(1,i.width*s);c.height=Math.max(1,i.height*s);c.getContext("2d").drawImage(i,0,0,c.width,c.height);resolve(c.toDataURL("image/jpeg",.82))};i.onerror=reject;i.src=e.target.result};r.onerror=reject;r.readAsDataURL(file)})}
foto.onchange=async()=>{try{const v=await compress(foto.files[0]);if(v){fotoPreview.src=v;fotoPreview.style.display="block"}}catch{status(adminStatus,"Erro ao processar a foto.","erro")}};$("apoiadorImagem").onchange=async()=>{try{const v=await compress($("apoiadorImagem").files[0],700);if(v){$("apoiadorPreview").src=v;$("apoiadorPreview").style.display="block"}}catch{status(apoiadorStatus,"Erro ao processar a imagem do apoiador.","erro")}};
async function loadApoiadores(){try{let s;try{s=await getDocs(query(collection(db,"apoiadores"),orderBy("ordem")))}catch{s=await getDocs(collection(db,"apoiadores"))}apoiadores=s.docs.map(x=>({id:x.id,...x.data()})).sort((a,b)=>(Number(a.ordem)||999)-(Number(b.ordem)||999));renderApoiadores()}catch(e){console.error(e);status(apoiadorStatus,`Erro ao carregar apoiadores (${e.code||"erro"}).`,"erro")}}
function renderApoiadores(){apoiadoresAdmin.innerHTML="";if(!apoiadores.length){apoiadoresAdmin.innerHTML='<p class="subtitulo">Nenhum apoiador cadastrado.</p>';return}apoiadores.forEach(a=>{const d=document.createElement("div");d.className="supporter-admin-item";d.innerHTML=`<img src="${esc(a.imagem||"")}" alt="${esc(a.nome)}"><div><strong>${esc(a.nome)}</strong><small>${esc(a.link||"")}</small><small>${a.ativo===false?"🔴 Oculto na página pública":"🟢 Visível na página pública"} · Ordem ${Number(a.ordem)||1}</small></div><div class="supporter-admin-actions"><button class="btn-secondary editar-apoiador" type="button">Editar</button><button class="btn-danger excluir-apoiador" type="button">Excluir</button></div>`;d.querySelector(".editar-apoiador").onclick=()=>editApoiador(a.id);d.querySelector(".excluir-apoiador").onclick=()=>removeApoiador(a.id);apoiadoresAdmin.appendChild(d)})}
function editApoiador(id){const a=apoiadores.find(x=>x.id===id);if(!a)return;$("apoiadorId").value=id;$("apoiadorNome").value=a.nome||"";$("apoiadorLink").value=a.link||"";$("apoiadorPlano").value=a.plano||"Ouro";$("apoiadorAtivo").checked=a.ativo!==false;apoiadorImagemAtual=a.imagem||"";if(apoiadorImagemAtual){$("apoiadorPreview").src=apoiadorImagemAtual;$("apoiadorPreview").style.display="block"}$("btnSalvarApoiador").textContent="Salvar apoiador";$("btnCancelarApoiador").classList.remove("hidden");$("apoiadoresView").scrollIntoView({behavior:"smooth"})}
async function loadApoiadoresPendentes(){if(!apoiadoresPendentes)return;apoiadoresPendentes.innerHTML="<p class=\"subtitulo\">Carregando solicitações...</p>";try{const s=await getDocs(collection(db,"apoiadores_pendentes"));const arr=s.docs.map(d=>({id:d.id,...d.data()})).sort((a,b)=>String(b.criadoEm||"").localeCompare(String(a.criadoEm||"")));if(apoiadoresPendentesBadge)apoiadoresPendentesBadge.textContent=arr.length;if(!arr.length){apoiadoresPendentes.innerHTML="<p class=\"subtitulo\">Nenhuma solicitação pendente.</p>";return}apoiadoresPendentes.innerHTML=arr.map(a=>`<div class="supporter-pending-item"><div><strong>${esc(a.empresa||"Sem empresa")}</strong><span>${esc(a.plano||"Plano")} · R$ ${Number(a.valor||0).toFixed(2).replace(".",",")} / mês</span><small>${esc(a.responsavel||"")} · ${esc(a.contato||"")} · ${esc(a.cidade||"")}</small><small>Status: ${esc(a.status||"aguardando_pagamento")}</small></div><div class="supporter-admin-actions"><button class="btn-primary confirmar-apoio" type="button">💰 Confirmar pagamento e publicar</button><button class="btn-danger recusar-apoio" type="button">Recusar</button></div></div>`).join("");arr.forEach(a=>{const el=[...apoiadoresPendentes.querySelectorAll(".supporter-pending-item")].find(x=>x.querySelector(".confirmar-apoio"));if(!el)return});[...apoiadoresPendentes.querySelectorAll(".supporter-pending-item")].forEach((el,i)=>{el.querySelector(".confirmar-apoio").onclick=()=>aprovarApoiadorPendente(arr[i]);el.querySelector(".recusar-apoio").onclick=()=>recusarApoiadorPendente(arr[i])})}catch(e){console.error(e);status(apoiadoresPendentesStatus,"Não foi possível carregar as solicitações.","erro")}}async function aprovarApoiadorPendente(a){if(!confirm(`Confirmar pagamento de "${a.empresa}" e publicar no plano ${a.plano}?`))return;try{const valores={Bronze:50,Prata:100,Ouro:200,Master:350};const data={nome:a.empresa,link:normalizeUrl(a.link||"#"),imagem:a.imagem||"",plano:a.plano||"Bronze",valor:Number(a.valor||valores[a.plano]||50),ativo:true,ordem:1,origem:"solicitacao_publica",contatoApoiador:a.contato||"",cidade:a.cidade||"",atualizadoEm:serverTimestamp(),criadoEm:a.criadoEm||serverTimestamp()};await setDoc(doc(db,"apoiadores",a.id),data,{merge:true});await deleteDoc(doc(db,"apoiadores_pendentes",a.id));status(apoiadoresPendentesStatus,"Pagamento confirmado e apoiador publicado. Agora adicione a logo no cadastro do apoiador.");await loadApoiadoresPendentes();await loadApoiadores()}catch(e){console.error(e);status(apoiadoresPendentesStatus,`Erro ao publicar (${e.code||"erro"}).`,"erro")}}async function recusarApoiadorPendente(a){if(!confirm(`Recusar a solicitação de "${a.empresa}"?`))return;try{await deleteDoc(doc(db,"apoiadores_pendentes",a.id));status(apoiadoresPendentesStatus,"Solicitação recusada.");await loadApoiadoresPendentes()}catch(e){console.error(e);status(apoiadoresPendentesStatus,`Erro ao recusar (${e.code||"erro"}).`,"erro")}}async function removeApoiador(id){const a=apoiadores.find(x=>x.id===id);if(!a||!confirm(`Excluir o apoiador "${a.nome}"?`))return;try{await deleteDoc(doc(db,"apoiadores",id));status(apoiadorStatus,"Apoiador excluído com sucesso.");await loadApoiadores()}catch(e){status(apoiadorStatus,`Erro ao excluir (${e.code||"erro"}).`,"erro")}}
loginForm.onsubmit=async e=>{e.preventDefault();clear(loginStatus);try{await signInWithEmailAndPassword(auth,$("loginEmail").value.trim().toLowerCase(),$("loginPassword").value)}catch(err){let m=err.code==="auth/invalid-credential"?"E-mail ou senha incorretos.":err.code==="auth/unauthorized-domain"?"Domínio não autorizado no Firebase.":`Erro no login (${err.code||"desconhecido"}).`;status(loginStatus,m,"erro")}};
const forgot=document.createElement("button");forgot.type="button";forgot.textContent="Esqueci minha senha";forgot.style.cssText="background:none;border:0;color:#93c5fd;cursor:pointer;text-decoration:underline;padding:8px 0";loginForm.appendChild(forgot);forgot.onclick=async()=>{const e=$("loginEmail").value.trim();if(!e)return status(loginStatus,"Digite seu e-mail primeiro.","erro");try{await sendPasswordResetEmail(auth,e);status(loginStatus,"Link de redefinição enviado.")}catch(err){status(loginStatus,`Erro (${err.code||"desconhecido"}).`,"erro")}};
$("btnSair").onclick=()=>signOut(auth);$("btnAtualizarMonetizacao")?.addEventListener("click",loadMonetizacao);$("btnAtualizarEquipes")?.addEventListener("click",loadEquipesPendentes);$("btnAtualizarNovosCadastros")?.addEventListener("click",async()=>{const b=$("btnAtualizarNovosCadastros");if(b){b.disabled=true;b.textContent="🔄 Atualizando..."}try{await loadNovosCadastros()}finally{if(b){b.disabled=false;b.textContent="🔄 Atualizar"}}});$("fecharNovoModal")?.addEventListener("click",fecharPerfilNovo);$("novoCadastroModal")?.addEventListener("click",e=>{if(e.target.matches("[data-fechar-novo-modal]"))fecharPerfilNovo()});$("btnAdicionarHistorico").onclick=()=>addHistorico();$("btnCancelar").onclick=resetForm;$("btnCancelarApoiador").onclick=resetApoiadorForm;notasIds.forEach(n=>$("nota"+n).addEventListener("change",atualizarMedia));
document.querySelectorAll(".admin-tab").forEach(btn=>btn.onclick=()=>{document.querySelectorAll(".admin-tab").forEach(x=>x.classList.remove("active"));document.querySelectorAll(".admin-view").forEach(x=>x.classList.remove("active"));btn.classList.add("active");$(btn.dataset.tab).classList.add("active")});
atletaForm.onsubmit=async e=>{e.preventDefault();clear(adminStatus);const nome=$("nome").value.trim(),time=$("time").value.trim();if(!nome||!time)return status(adminStatus,"Preencha nome completo e time atual.","erro");$("btnSalvar").disabled=true;try{let f=fotoAtual;if(foto.files[0])f=await compress(foto.files[0]);const data={nome,nascimento:$("nascimento").value,cidade:normalizarCidade(cidadeComUF()),modalidades:normalizarModalidades([...document.querySelectorAll('#atletaForm input[name="modalidade"]:checked')].map(x=>x.value)),modalidade:normalizarModalidades([...document.querySelectorAll('#atletaForm input[name="modalidade"]:checked')].map(x=>x.value)).join(", "),posicoes:getPosicoes(),posicao:getPosicoes().join(", "),categoria:normalizarCategoria($("categoria").value),time,status:$("status").value,historicoEquipes:getHistorico(),historicoCampeonatos:getCampeonatos(),observacoes:$("observacoes").value.trim(),foto:f,avaliacaoTecnica:getAvaliacao(),atualizadoEm:serverTimestamp()};let id=atletaId.value;if(id)await updateDoc(doc(db,"atletas",id),data);else id=(await addDoc(collection(db,"atletas"),{...data,criadoEm:serverTimestamp()})).id;await setDoc(doc(db,"atletas",id,"privado","dados"),{contato:$("contato").value.trim(),atualizadoEm:serverTimestamp()},{merge:true});status(adminStatus,atletaId.value?"Atleta atualizado com sucesso.":"Atleta cadastrado com sucesso.");resetForm();await loadAtletas()}catch(err){console.error(err);status(adminStatus,`Não foi possível salvar (${err.code||"erro"}). Verifique as regras do Firestore.` ,"erro")}finally{$("btnSalvar").disabled=false}};
apoiadorForm.onsubmit=async e=>{e.preventDefault();clear(apoiadorStatus);const nome=$("apoiadorNome").value.trim(),link=normalizeUrl($("apoiadorLink").value),id=$("apoiadorId").value,plano=$("apoiadorPlano").value;if(!nome||!link)return status(apoiadorStatus,"Informe o nome e o link do apoiador.","erro");if(!apoiadorImagemAtual&&!$("apoiadorImagem").files[0])return status(apoiadorStatus,"Escolha a imagem ou logo do apoiador.","erro");$("btnSalvarApoiador").disabled=true;try{let imagem=apoiadorImagemAtual;if($("apoiadorImagem").files[0])imagem=await compress($("apoiadorImagem").files[0],700);const valores={Bronze:50,Prata:100,Ouro:200,Master:350};const ordemPorPlano={Master:1,Ouro:2,Prata:3,Bronze:4};const data={nome,link,imagem,plano,valor:Number(valores[plano]||200),ativo:$("apoiadorAtivo").checked,ordem:ordemPorPlano[plano]||4,atualizadoEm:serverTimestamp()};if(id)await updateDoc(doc(db,"apoiadores",id),data);else await addDoc(collection(db,"apoiadores"),{...data,criadoEm:serverTimestamp()});status(apoiadorStatus,id?"Apoiador atualizado com sucesso.":"Apoiador adicionado com sucesso.");resetApoiadorForm();await loadApoiadores()}catch(err){console.error(err);status(apoiadorStatus,`Não foi possível salvar (${err.code||"erro"}). Verifique as regras do Firestore.` ,"erro")}finally{$("btnSalvarApoiador").disabled=false}};
$("btnAdicionarCampeonato").onclick=()=>addCampeonato();$("btnAdicionarHistorico").onclick=()=>addHistorico();onAuthStateChanged(auth,async u=>{if(u){loginSection.classList.add("hidden");adminSection.classList.remove("hidden");$("usuarioLogado").textContent=`Administrador conectado: ${u.email}`;resetForm();resetApoiadorForm();await Promise.allSettled([loadAtletas(),loadApoiadores(),loadApoiadoresPendentes(),loadNovosCadastros(),loadEquipesPendentes(),loadMonetizacao(),loadReivindicacoes()]);if($("novosCadastrosLista")&&$("novosCadastrosLista").textContent.trim()==="Carregando...")await loadNovosCadastros()}else{loginSection.classList.remove("hidden");adminSection.classList.add("hidden")}});


async function loadReivindicacoes() {
  const lista = $("reivindicacoesLista");
  const badge = $("reivindicacoesBadge");
  if (!lista) return;
  try {
    const [claimsSnapshot, athletesSnapshot] = await Promise.all([
      getDocs(query(collection(db, "reivindicacoes_perfis"), where("status", "==", "pendente"))),
      getDocs(collection(db, "atletas")),
    ]);
    const athletes = athletesSnapshot.docs.map((item) => ({ id: item.id, ...item.data() }));
    const claims = claimsSnapshot.docs.map((item) => ({ id: item.id, ...item.data() })).filter((claim) => athletes.some((athlete) => athlete.id === claim.perfilId && !athlete.ownerUid));
    const unlinked = athletes.filter((athlete) => !athlete.ownerUid).sort((a, b) => String(a.nome || "").localeCompare(String(b.nome || ""), "pt-BR"));
    if (badge) badge.textContent = String(claims.length);
    const claimsHtml = claims.length ? claims.map((claim) => `<article class="atleta-admin claim-card"><div class="atleta-info"><strong>${esc(claim.perfilNome || "Perfil sem nome")}</strong><span>Solicitante: ${esc(claim.solicitanteNome || "Nome não informado")}</span><small>${esc(claim.solicitanteEmail || "E-mail não informado")} · UID: ${esc(claim.solicitanteUid || "não informado")}</small></div><div class="atleta-actions"><button class="btn-primary claim-approve" data-id="${esc(claim.id)}" type="button">Vincular e aprovar</button><button class="btn-danger claim-reject" data-id="${esc(claim.id)}" type="button">Recusar</button></div></article>`).join("") : '<p class="subtitulo">Nenhuma reivindicação pendente.</p>';
    const unlinkedHtml = unlinked.length ? `<div class="legacy-profiles"><h3>Perfis sem login vinculado (${unlinked.length})</h3><p class="subtitulo">Selecione um perfil para preencher o ID no vínculo manual abaixo.</p>${unlinked.slice(0, 100).map((athlete) => `<div class="atleta-admin"><div class="atleta-info"><strong>${esc(athlete.nome || "Sem nome")}</strong><span>${esc(athlete.cidade || "Cidade não informada")} · ${esc(athlete.categoria || "Categoria não informada")}</span><small>ID: ${esc(athlete.id)}</small></div><div class="atleta-actions"><button class="btn-secondary choose-profile" data-profile-id="${esc(athlete.id)}" type="button">Usar este ID</button></div></div>`).join("")}</div>` : '<p class="subtitulo">Não há perfis legados sem vínculo.</p>';
    lista.innerHTML = `<div class="pending-claims"><h3>Solicitações aguardando análise (${claims.length})</h3>${claimsHtml}</div>${unlinkedHtml}`;
    lista.querySelectorAll(".claim-approve").forEach((button) => button.onclick = () => aprovarReivindicacao(claims.find((claim) => claim.id === button.dataset.id)));
    lista.querySelectorAll(".claim-reject").forEach((button) => button.onclick = () => recusarReivindicacao(claims.find((claim) => claim.id === button.dataset.id)));
    lista.querySelectorAll(".choose-profile").forEach((button) => button.onclick = () => { $("vinculoPerfilId").value = button.dataset.profileId; $("vinculoUid")?.focus(); });
    status($("reivindicacoesStatus"), `${claims.length} solicitação${claims.length === 1 ? "" : "ões"} pendente${claims.length === 1 ? "" : "s"}; ${unlinked.length} perfil${unlinked.length === 1 ? "" : "is"} sem vínculo.`);
  } catch (error) {
    console.error("Erro ao carregar reivindicações:", error);
    if (badge) badge.textContent = "!";
    status($("reivindicacoesStatus"), `Não foi possível carregar as reivindicações (${error.code || "erro"}).`, "erro");
  }
}

async function aprovarReivindicacao(claim) {
  if (!claim) return;
  if (!confirm(`Vincular o perfil “${claim.perfilNome || "Atleta"}” à conta ${claim.solicitanteEmail || claim.solicitanteUid}?`)) return;
  try {
    const athleteRef = doc(db, "atletas", claim.perfilId);
    const athleteSnapshot = await getDoc(athleteRef);
    if (!athleteSnapshot.exists()) throw new Error("Perfil não encontrado.");
    const athlete = athleteSnapshot.data();
    if (athlete.ownerUid) throw new Error("Este perfil já possui uma conta vinculada.");
    await updateDoc(athleteRef, { ownerUid: claim.solicitanteUid, ownerEmail: claim.solicitanteEmail || "", ownerDisplayName: claim.solicitanteNome || "", atualizadoEm: serverTimestamp() });
    await updateDoc(doc(db, "reivindicacoes_perfis", claim.id), { status: "aprovada", analisadoPorUid: auth.currentUser.uid, analisadoEm: serverTimestamp() });
    await loadReivindicacoes();
    status($("reivindicacoesStatus"), "Perfil vinculado e reivindicação aprovada.");
  } catch (error) {
    console.error("Erro ao aprovar reivindicação:", error);
    status($("reivindicacoesStatus"), error.message || "Não foi possível vincular o perfil.", "erro");
  }
}

async function recusarReivindicacao(claim) {
  if (!claim || !confirm("Recusar esta solicitação de reivindicação?")) return;
  try {
    await updateDoc(doc(db, "reivindicacoes_perfis", claim.id), { status: "recusada", analisadoPorUid: auth.currentUser.uid, analisadoEm: serverTimestamp() });
    await loadReivindicacoes();
  } catch (error) {
    console.error("Erro ao recusar reivindicação:", error);
    status($("reivindicacoesStatus"), "Não foi possível recusar a solicitação.", "erro");
  }
}

$("btnAtualizarReivindicacoes")?.addEventListener("click", loadReivindicacoes);
$("vinculoManualForm")?.addEventListener("submit", async (event) => {
  event.preventDefault();
  const profileId = $("vinculoPerfilId").value.trim();
  const uid = $("vinculoUid").value.trim();
  const output = $("vinculoManualStatus");
  if (!profileId || !uid) return status(output, "Informe o ID do perfil e o UID da conta.", "erro");
  const button = event.currentTarget.querySelector("button[type=submit]");
  button.disabled = true;
  try {
    const athleteRef = doc(db, "atletas", profileId);
    const athleteSnapshot = await getDoc(athleteRef);
    if (!athleteSnapshot.exists()) throw new Error("Perfil não encontrado.");
    const athlete = athleteSnapshot.data();
    if (athlete.ownerUid) throw new Error("Este perfil já está vinculado a uma conta.");
    const userSnapshot = await getDoc(doc(db, "usuarios", uid));
    const user = userSnapshot.exists() ? userSnapshot.data() : {};
    await updateDoc(athleteRef, { ownerUid: uid, ownerEmail: user.email || "", ownerDisplayName: user.nome || "", atualizadoEm: serverTimestamp() });
    await setDoc(doc(db, "reivindicacoes_perfis", `manual_${profileId}_${uid}`), { perfilId: profileId, perfilNome: athlete.nome || "Atleta", solicitanteUid: uid, solicitanteEmail: user.email || "", solicitanteNome: user.nome || "", status: "aprovada", origem: "manual_admin", analisadoPorUid: auth.currentUser.uid, analisadoEm: serverTimestamp() }, { merge: true });
    event.currentTarget.reset();
    status(output, "Perfil vinculado manualmente com sucesso.");
    await loadReivindicacoes();
  } catch (error) {
    console.error("Erro no vínculo manual:", error);
    status(output, error.message || "Não foi possível vincular o perfil.", "erro");
  } finally { button.disabled = false; }
});
